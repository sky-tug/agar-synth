# AGAR-SYNTH bagimliliklari
# PyTorch BURADA YOK -- CUDA surumune gore ayri kuruluyor (scripts/setup.sh)

# --- Faz 1-2: veri hatti, tespit, olcum ---
# Karar 3.46: >=8.3.0 idi. Ama base.yaml yolo26n.pt ve cutmix istiyor, ikisi de
# 8.4'te geldi -- yani eski kisit, config'i CALISTIRAMAYACAK bir surume izin
# veriyordu. Olcumler 8.4.118 ile yapildi; Faz 5'te `pip freeze > requirements.lock`.
ultralytics>=8.4.118
pycocotools>=2.0.7        # olcum kodunun capraz dogrulamasi (test_metrics.py test 10)
numpy>=1.26
pandas>=2.0
pillow>=10.0
opencv-python-headless>=4.9
pyyaml>=6.0
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
tqdm

# --- deney takibi ---
wandb>=0.16

# --- Faz 2: klasik veri artirma kolu (Baseline B/C) ---
albumentations>=1.4

# --- Faz 3: uretim hatti (difuzyon) -- 26 Agustos: ACILDI ---
# src/generate/ icinde GERCEKTEN import edilenler: diffusers, peft.
# transformers/accelerate/safetensors diffusers'in calisma zamani bagimliligi
# (SD 1.5 inpainting metin kodlayicisi icin); acikca yaziliyor ki makale
# yeniden uretilebilir olsun. Kurulu ve olcumlerin yapildigi surumler:
# diffusers 0.39.0, peft 0.20.0.
diffusers>=0.39
peft>=0.20
transformers>=4.40
accelerate>=0.30
safetensors

# --- Plan listesindeydi, KODA HIC GIRMEDI (26 Agustos import taramasi) ---
# Gerceklik olcumu su an species_check.py'nin kendi olcutleriyle yapiliyor.
# Faz 8'de FID/LPIPS eklenirse burasi acilir ve karar decisions.md'ye yazilir.
# clean-fid                # FID / KID
# lpips                    # LPIPS

# --- Faz 7: XAI kolu (Grad-CAM/++) ---
# grad-cam>=1.5
